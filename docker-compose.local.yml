services:
  mongodb:
    image: mongo:7
    container_name: growsim-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: growsim
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
